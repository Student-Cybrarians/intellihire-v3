# The project pins next@14.2.x while @cloudflare/next-on-pages declares a
# peer of next>=14.3.0. We intentionally stay on the tested 14.2 line, so
# accept that peer mismatch (same flag the app was originally installed with).
legacy-peer-deps=true
